package observerpattern.solution;


public interface Observer {

    void update();// updates the concrete observers according to the trigger from the concrete subject
                 // it observes

}
