package abstractfactory.solution;

public interface Window {
     /**
      * a window interface
      * could be implemented with any type of window products we wish
      * can contain any logic related to a window representation
      */
}
